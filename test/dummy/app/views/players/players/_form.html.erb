<!-- Generated by Hecks On Rails -->

<%= form_with(model: players_player, local: true) do |form| %>
  <% if players_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(players_player.errors.count, "error") %> prohibited this players_player from being saved:</h2>

      <ul>
      <% players_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>